# 2026-09-17

## Geteilten Task gelesen (workbuddy.cc … /share/kUXVGEwtty) — und Ist-Stand verifiziert

**Inhalt des geteilten Tasks** (Titel „Verify Next.js build and visual regression", letzter Lauf
02:30, Dienst nicht verfügbar): Gina/Jeremy wollte den **mittleren Bereich der App-Seite in *eine*
Card** — der Agent hatte stattdessen jede Komponente einzeln in Karten gepackt (`.ptitle`, `.chips`,
`.metrics`, `.rows`, `table.tbl`, `.duo .box`) → „stoppppp nicht jede komponente in cards man sondern
im gesamten die mittlere bereich in eine card". Danach: AppShell-Bau (Topbar + Hauptmenü +
Unterpunkte + rechte Spalte) zerschoss das Layout (Hauptmenü überlappte die Adresse), **Fehler nur im
echten Server-Screenshot, nicht im `next build`** → `git revert HEAD` auf `957b57d`, Seite wieder gut,
Gates grün, beide Commits bleiben in der Historie. Merksatz: *Mockup im Vergleich ist nicht Mockup im
Designsystem; `EHWorkspaceFrame` ist ein 80-px-Flexcontainer mit drei Kindern — sechs Hauptmenü-Pillen
passen nicht zwischen Logo und Tools.* Vor jedem AppShell-Push: **echter Server-Screenshot als Beleg.**
Offene Frage an den Agenten (02:16, blieb unbeantwortet — Credits 429): was tun, damit **günstige
Agenten** (Subagents, DeepSeek V4.1 Flash) das simple fortführen können — `DESIGN.md` und
Komponentenvorlagen fixen.

**Verifizierter Ist-Stand (02:37):**
- Live `https://einfachhausen.de/api/health` → `ok:true`, `state:ready`, alle Pflicht-Checks grün,
  `uptime_seconds` **2546** → Prozessstart **~01:54**. Also: **live ist gesund.**
- `main`/`origin/main` = **`8f20654`** („Hauptmenue als eigene Zeile, Unterpunkte in die Seitenleiste,
  rechte Spalte", 02:05:18) — das ist genau der *saubere* Nachfolgebau des zerschossenen Versuchs
  (10 Dateien, +158/−32). Der Commit ist **11 Minuten jünger als der Live-Prozessstart → `8f20654` ist
  nicht live.** Live läuft der Revert-Stand.
- Lokaler Arbeitsbaum: `.next` von 02:04; offene Altlasten `?? .next.bak-vor-seo-pruefung/`,
  `?? tmp-deadcss.mjs`; Branch `feature/cleanup-dead-trust-classes` zeigt auf `8f20654`.
- `DESIGN.md` 35 KB (16.09. 16:00) = Designsystem 1.0; `design/design-debt.json` 120 KB (Ratsche) und
  `design-lock.json` wurden mit `8f20654` mitgezogen (02:03).

**Konsequenz:** Der Schaden-Verdacht („zustand sieht zerstört aus") betrifft **nicht** die Live-Seite,
sondern den damals deployten Zwischenstand. Der ungeprüfte Teil ist `8f20654` auf `main` — er braucht
vor einem Deploy einen **Sichtbeleg**, keine weitere Build-Prüfung.

## Reparatur `vergleich/werkbank-vergleich/vergleich.html`

Auftrag: „wurde offenbar durch einen Agenten optisch beschädigt" — analysieren, beheben,
ursprüngliches Layout wiederherstellen. **Kein Git im Workspace, kein Backup vorhanden.**

**Vorgehen, das sich bewährt hat:**

1. Render-Beleg per Playwright (`playwright-core` 1.63.0 aus dem Projekt +
   Chromium 1187 aus `~/Library/Caches/ms-playwright/`) — nicht nur Datei lesen.
2. **Detektor-Skript statt Augenmaß:** im Browser nach Block-Elementen suchen, deren Eltern noch
   Fließtext enthält → findet erzwungene Umbrüche objektiv.
3. Geometrie messen: `getBoundingClientRect` + `getComputedStyle` je Grid-Spalte.
4. **Referenz suchen:** `vergleich/ziel/a-liste.html` ist der kanonische Ziel-Prototyp der
   Werkbank-Richtung. Dort ist `.main` **ohne** Background/Border/Radius/Shadow — das war der
   Beweis für den Schaden in `.mmain`.

**Drei Fehler, alle in vergleich.html, alle behoben:**

| # | Stelle | Fehler | Fix |
|---|---|---|---|
| 1 | Z. 32 `.adopt b` | traf auch `<b>` **in** den `<li>` → Liste brach mitten im Satz um („Hauptmenü oben" / neue Zeile beginnt mit Komma) | `.adopt > b` (Kindselektor) |
| 2 | Z. 105 `.mmain` | Karten-Optik (`border` + `border-radius:10px` + `box-shadow`) auf einer Grid-Spalte mit **gap 0** zu `.mside`/`.mrail` → ausgefressene Ecken, doppelte Trennlinien | `border`/`border-radius`/`box-shadow` entfernt, `padding` bleibt |
| 3 | Z. 82 Media-Query | `.mrail{display:none}` wirkungslos (Z. 180 setzt später `display:flex`) + widersprüchliche Spaltendefinition | auf `198px 1fr 220px` bzw. `.no-rail` zweispaltig bereinigt |

**Belege:** `/tmp/eh-vorher-*.png`, `/tmp/eh-zoom-naht.png` (vorher/nachher), Detektor vorher
4 Treffer / nachher 0. Backup: `vergleich.html.bak-vor-reparatur-20260917-0245`.

**Offen gemeldet, nicht gefixt:** bei 900 px Viewport 80 px horizontaler Überlauf
(`scrollW 980`), Ursache `.mtop`/`.ws-nav`/`.ws-find{min-width:170px}` — die Topbar kann nicht
schrumpfen. Ab 980 px sauber. Nicht durch die Reparatur entstanden, war vorher genauso.

## Wichtig für später
- **`data-page-node-id` auf allen Elementen ist Projektstandard** (in 12 Vergleichs-HTMLs) —
  Artefakt des Editors, **kein** Schaden. Nicht als Fehler jagen.
- **`ist/app/` enthält 31 Screenshots** (15 Routen × Desktop/Mobile) vom 17.09. 01:49 —
  die Datei referenziert nur 6 Desktop-Aufnahmen.

## Umstellungsplan + Bestandsaufnahme (03:00–03:30)

Plan als `UMSTELLUNGSPLAN.md` im Workspace. Ausgeführt: **Welle A** (Bestandsaufnahme,
3 Subagents, DeepSeek V4.1 Flash, rein lesend).

**Zukunftssicherung: die vier als blockierend bezeichneten Punkte stehen schon.**
§4 Schichtung (`globals.css:1` `@layer eh-tokens, …, eh-pages`), §1.2 Whitelist
(`RAW_PROPS`/`RAW_OK` in `eh-design-check.mjs:14-17`), §2 Ratsche (`debtCap` 8314 → **0 bis
30.04.2027**), Tot-Liste (`design/design-deadcss.json`). Der Seitenumbau ist **nicht mehr blockiert**.

**Die harte Grenze:** `design/design-lock.json` versiegelt **54 Dateien** — u. a.
`packages/eh-design/**` (AppShell `workspace.tsx`, `styles.module.css`, `tokens.*`), `DESIGN.md`,
`design/*.json`, `src/app/globals.css`, `src/app/design-system.css`, beide Bereichs-CSS-Module,
`src/components/marketing/**`. **Frei:** `src/app/app/**/page.tsx`, `src/app/pro/**/page.tsx`,
`src/app/admin/**`, `src/components/shell.tsx`, `shell.module.css`, `nav-config.ts`.
→ **Der Seitenumbau findet in freien Dateien statt; die AppShell braucht Freigabe + Seal.**

**Bestandsaufnahme (Zeilenzahlen, Abstand zur Werkbank):**

| Bereich | Routen | Befund |
|---|---|---|
| `/app` | 21 | Topmenü + Shell-Sidebar da; **rechte Spalte nur bei 3** (`/app`, `home`, `home/history`); die Werkbank-Struktur liegt als **lokales `<style>` in `src/app/app/page.tsx`**, nicht in der Bibliothek |
| `/pro` | 12 | weiter: `/pro` hat a+c+d; 4 Kennzahlen oft da; **rechte Spalte fehlt fast überall** |
| `/admin` | 4 | hängt **nicht** an der AppShell, nur `EHAppHeader`; `crm/page.tsx` = 166 Verstöße (109 Hex, Tailwind-Farben) |

**Schulden-Befund:** `8f20654` hat **11 neue Verstöße** erzeugt und die Ratsche nachgezogen
statt den Code zu korrigieren — `src/app/app/page.tsx` 6× `small-type` + 1× `unowned-style`,
`src/components/shell.module.css` 1× `small-type` + 3× `raw-value`. Gesamtstand **6240 von 8314**
(73 Dateien), Rest 2074. **Muster, das kein Subagent wiederholen darf.**

**Toter Code:** `src/app/pro/provider-workspace.module.css` = **1958 Zeilen, 379 Selektoren**, aber
als Modul nur von `layout.tsx` importiert (2 Klassen) und von den Seiten nur über ~15 globale Namen
genutzt. `src/app/admin/admin.module.css` dagegen 235 Zeilen, 19 Selektoren, **0 tote**.

**Zielvorlage:** `vergleich/ziel/a-liste.png` (Desktop, 16.09.) = Dichte-Messlatte (vier Kennzahlen,
Chips, sechs dichte Zeilen, rechte Spalte). **Teilweise überholt:** dort liegt das Hauptmenü noch in
der Sidebar — seit `8f20654` gehört es als eigene Zeile nach oben.

**Welle 0 bleibt blockierend:** `8f20654` ist nicht live und braucht einen echten Server-Screenshot.
Weg steht in der Skill `eh-app-sichtpruefung` (Screenshots nur auf dem Deploy-Host, danach
`tests/visual-baselines/app` aufräumen, sonst „dirty working tree" beim nächsten Deploy).

## Soll-Vorschau repariert + erste Umbau-Welle (03:30–04:20)

**`vergleich/werkbank-vergleich/vergleich.html` — zwei weitere Fehler gefunden und behoben:**

| Stelle | Fehler | Fix |
|---|---|---|
| Topbar | Braucht **921 px** Mindestbreite (Logo + 6 Menüpunkte + Werkzeuge). Unter ~980 px läuft die Seite über — und **der Ist-Screenshot erbt die Breite** (`.shot img` wird 923 px statt Containerbreite). Genau das passiert in einem schmalen Vorschau-Panel. | Media Query 1120 px: Hauptmenü als eigene Zeile (dieselbe Lösung wie `8f20654`), Suche ohne `min-width` |
| Rechte Spalte | Unter 980 px quillt jede Tabelle unter der Rail hervor statt umzubrechen | Media Query 980 px: Rail weg (Schwelle wie in der App „unter 900 px"), Metriken 2-spaltig, Duo 1-spaltig, `.mc{overflow-x:auto}` |

**Jetzt sauber bei 1440 / 1000 / 820 / 600 px** (`scrollW == clientW`), 0 erzwungene Umbrüche,
0 Rasterprobleme. Alle sechs Soll-Blöcke einzeln geprüft — inhaltlich waren sie nie defekt.

**Umbau-Welle 1 (Branch `design/werkbank-umstellung-20260917`, 7 Seiten, +484/−91):**
`app/documents`, `app/calendar`, `app/year`, `app/profile`, `app/hausmeister`, `pro/hilfe`,
`pro/plans`. Muster je Seite: `EHMetricsBar` auf **vier** echte Kennzahlen +
`EHWorkspaceGrid main={…} aside={…}` als rechte Spalte. `tsc --noEmit` durchweg Exit 0,
Ratsche (`design/`) unberührt.

**DER WICHTIGSTE BEFUND — die Ratsche blockiert das Aufräumen:**
`pro/plans` hatte **1** gedeckten `state-class`-Verstoß (`className="current-plan pro-current-plan"`).
Regel 5 verlangt seine Entfernung — danach meldet der Check
`debt baseline is stale for state-class (0 < 1); run design:debt:sync` und **schlägt fehl**.
`design/design-debt.json` ist aber **versiegelt**. Heißt: **Ohne Freigabe kann niemand Schuld
abbauen.** Das ist die Ratsche, wie sie gedacht ist — aber es ist ein Blocker für jeden Umbau,
der Altlasten entfernt. Der Sync ist ein eigener, freigabepflichtiger Schritt.

**Weitere harte Fakten für den Umbau:**
- `./node_modules/.bin/tsc --noEmit` bricht ohne `NODE_OPTIONS=--max-old-space-size=6144` mit
  SIGTERM/137 ab.
- `EHWorkspaceGrid main={…} aside={…}` ist die rechte Spalte; `EHMetricsBar label="…" items={[…]}`
  die Kennzahlenleiste. Beide aus `@/design-system`.
- Subagenten dürfen **genau eine Datei** ändern — sonst ist `git status` nicht mehr prüfbar und
  parallele Agenten überschreiben sich.
- **Offen: 30 weitere Seiten** (`/app` 21, `/pro` 12, `/admin` 4, minus die 7 erledigten).

## Umbau abgeschlossen — 34 Dateien (04:30–08:40)

**Alle App-Seiten stehen auf der Werkbank-Struktur.** Branch `design/werkbank-umstellung-20260917`,
**34 Dateien, +1976/−619**. `NODE_OPTIONS=--max-old-space-size=6144 ./node_modules/.bin/tsc --noEmit`
→ **0 Fehler**. **Keine versiegelte Datei berührt** (`git diff --name-only` gegen die Lock-Liste: 0 Treffer).

Vorgehen: 11 Subagents (DeepSeek V4.1 Flash), jeder mit **genau einer bis sechs Dateien**, alle über die
neue Skill `eh-werkbank-seitenumbau` gebrieft. Je Seite vier echte Kennzahlen (`EHMetricsBar`) und eine
seitenspezifische rechte Spalte (`EHWorkspaceGrid main/aside`).

**Was die zentrale Typprüfung gefunden hat — und kein Subagent gemeldet hätte:**
1. `src/app/app/messages/page.tsx`: 5× `Property 'unreadCount' does not exist on type 'ContactView'` —
   Union-Typ, Feld nur in einem Zweig. Fix: zweiter Zweig gibt `{ ...contact, unreadCount: 0 }` zurück.
2. `src/app/app/home/history/page.tsx`: fehlende `}` nach dem `EHMetricsBar`-Block (Syntaxfehler, der
   vier Folgefehler verdeckte) + `reduce` über `new Map<string,number>()` inferierte `unknown`.
   Fix: explizite `Map`-Schleife statt `reduce`.
**Lehre: Subagents ohne `tsc` melden „sauber", obwohl es nicht kompiliert.** Die zentrale Typprüfung ist
nicht optional — und ein Syntaxfehler verdeckt die Fehler dahinter, also nach jedem Fix erneut prüfen.

**Sonderfälle, bewusst NICHT umgebaut** (begründet, keine Werkbank-Struktur nötig):
`app/invoices/[id]`, `app/home/passport`, `app/documents/[jobId]/receipt`, `pro/invoices/[id]`
(alle `EHDocumentFrame` — Beleg-/Druckansichten), `admin/login` (Anmeldeseite),
`app/partners` und `pro/jobs` (reine Redirects).

**BLOCKER, ungelöst:** `node scripts/eh-design-check.mjs` ist rot — **27 Meldungen**, alle
`debt baseline is stale`, aus zwei Dateien:
- `src/app/admin/crm/page.tsx`: **165 Verstöße entfernt** (109 Hex-Farben, 48 zu kleine Schriften,
  6 Tailwind-Farbklassen, 2 `rounded-full`) — die Seite ist jetzt vollständig auf Design-Tokens
- `src/app/pro/plans/page.tsx`: 1 `state-class` entfernt

Das ist der **Preis des Aufräumens**, nicht ein Fehler. Der Check bestraft jede Bereinigung, bis die
Baseline nachzieht. Nötig wäre `node scripts/eh-design-debt-sync.mjs` **plus** `eh-design-seal.mjs`
(die Baseline steht im Lock). Beides ist **freigabepflichtig** — der Sync-Kommentar sagt selbst:
„laeuft das nur mit Markenautoritaet auf main". Auf dem Branch bewusst **nicht** ausgeführt,
`design/` ist unverändert. Schuld sinkt dadurch rechnerisch um ~166 Punkte (6240 → ~6074).

**Noch offen außerdem:** Welle 0 (Sichtbeleg für `8f20654` und für den Umbau) — ohne den ist nichts
abgenommen. `8f20654` ist weiterhin **nicht live**.

## Abschluss: Sync, Seal, Commit, Push (11:15–11:35)

**Debt-Sync + Seal ausgeführt** (Jeremy hat mit „alles fertigmache" freigegeben).
`design-debt.json`: 6240 → **6074 Punkte in 71 Dateien**. Der Seal hat **genau einen** Hash geändert
(`design-debt.json`) — alle 53 übrigen Lock-Einträge blieben gleich, also wurde nichts mit-legalisiert.
`eh-design-check` → **`EH_DESIGN_CONSISTENT`, Exit 0**.

**Drei echte Fehler gefunden, die kein Subagent gemeldet hatte** (alle behoben):
1. `app/messages/page.tsx` — 5 Typfehler: `unreadCount` existiert nur in einem Zweig des Union-Typs.
2. `app/home/history/page.tsx` — fehlende `}`; der Syntaxfehler **verdeckte vier Folgefehler**
   (`reduce` über `new Map<string,number>()` inferierte `unknown`). Fix: explizite Map-Schleife.
3. `app/jobs/page.tsx` — ESLint-Error `react-hooks/purity`: neues `Date.now()` im Renderpfad.
   Fix: Berechnung in `nextUpcomingAppointment()` **außerhalb** des Components (wie `jobScheduleCopy`).
   Außerdem 4 ungenutzte Importe in `pro/messages` entfernt.
**Endstand: tsc 0 Fehler, ESLint 0 Errors, Designcheck grün.**

**Commit `59479ca`** auf `design/werkbank-umstellung-20260917` (36 Dateien), **gepusht**.
`origin/main` bleibt bei `8f20654`.

### Zwei Blocker, die jetzt gelten

**1. Ein PR würde scheitern — `design/**` muss direkt auf `main`.**
Simuliert mit `node scripts/eh-design-check.mjs --base 8f20654…`:
```
Brand authority required; protected path differs from trusted base: design/design-debt.json
Brand authority required; protected path differs from trusted base: design/design-lock.json
Debt baseline is immutable in ordinary PRs
```
Das ist Falle 38 in Aktion: **Die Baseline ist in normalen PRs unveränderlich.** Der Branch kann
deshalb **nicht** per PR gemergt werden — der Sync gehört per direktem Push auf `main`
(Markenautorität, wie bei `8f20654`). **Nicht selbst gemacht**, weil „Mergen tut Jeremy".

**2. Die CI läuft überhaupt nicht mehr — Billing.**
`gh run view` auf den Branch-Run: *„The job was not started because recent account payments have
failed or your spending limit needs to be increased."* Alle Jobs `steps=0` nach 3–5 s. Betrifft
auch die letzten drei Runs auf `main` (17.09. 00:05, 23:50, 23:45). **Es gibt also derzeit keine
automatische Verifikation** — die lokalen Prüfungen (tsc, ESLint, Designcheck) sind alles, was es
gibt. Das Billing-Problem liegt bei Jeremy.

## Ausgeliefert und gesehen (11:39–11:55)

Auf „fertig machen 100% alles" die ganze Kette gefahren — **`main` ist jetzt `16eb35a` und live**.

1. **Merge nach `main`** per direktem Push (`59479ca`, Fast-Forward von `8f20654`). Kein PR möglich,
   weil die Baseline dort unveränderlich ist (siehe Blocker 1 oben). 0 Löschungen im Commit geprüft.
2. **Deploy** über `deploy/update-on-oci.sh` — **Release-Gate 15/15**, Build PASS, axe-a11y PASS,
   visual canonicals PASS, Performance-Budgets PASS (CLS 0.0000, 1150 KB, 921 ms). Health `ready`.
3. **Sichtbeleg** per `app-visual-regression.mjs` auf dem Deploy-Host: **`APP VISUAL PASS`**,
   30 Bilder (16 owner / 10 provider / 4 admin, mobil+Desktop). Danach `tests/visual-baselines/app`
   und `app-visual-actual` gelöscht — Baum wieder sauber (`?? public/uploads`).

**Der Sichtbeleg hat sofort gefunden, was das Gate nicht sieht:** Die Startseite `/app` hatte nur
**drei** Kennzahlen (Termine, Dokumente, Aufträge). Die vierte Zahl war in Zeile 88 schon abgefragt
(`unread`), wurde aber nie angezeigt. Fix in `16eb35a`, erneut deployt, erneut belegt — jetzt
„Termine 1 | Dokumente 0 | Aufträge 1 | Ungelesen 1". **Genau der Fall, für den der Screenshot da ist.**

**Was der Beleg außerdem zeigt (offen, nicht gefixt):**
- `/app/jobs` und `/pro` tragen die Werkbank-Struktur sauber: vier Kennzahlen, Segment-Umschalter
  Liste/Karten/Chronik, Unterpunkte in der Seitenleiste, rechte Spalte mit „Nächster Termin".
- **`/app` hat zwei Werkzeugleisten übereinander:** die AppShell-Topbar (Hausmanager-Button, Suche,
  Glocke) und darunter der Objekt-Kopf mit Adresse, der **nochmal** Such- und Glocken-Icon trägt.
  Kommt aus `8f20654`, nicht aus dem Umbau — noch nicht angefasst.
- **`/pro` trägt weiter einen Marketingkopf** („Guten Morgen, Fixture." groß + Erklärtext). Das ist
  das „Dokument vs. Werkzeug"-Symptom aus dem Projektgedächtnis; die Zielvorlage `a-liste.png` hat
  dort einen kleinen Titel.
- **`/admin` ist per Screenshot nicht prüfbar** — die Aufnahmen zeigen die Login-Seite (byte-identisch
  mit den alten). Für den Verwaltungsbereich braucht es eine Admin-Session.

## Zwei Befunde behoben, einer offen (11:56–12:10)

**Commit `d227eb1`**, deployt, erneut belegt (`APP VISUAL PASS`, 30 Bilder, Gate 15/15):

1. **`/app`: doppelter Werkzeugkopf weg.** Suche und Glocke kamen zweimal — in der AppShell-Topbar
   (`src/components/shell.tsx:79`) und nochmal im Objektkopf der Seite (`/app/page.tsx:145-148`).
   Der Objektkopf trägt jetzt nur Adresse und Name; die tote Regel `.eh-werkbank-kopf-tools` ist mit
   entfernt. Im Screenshot bestätigt.
2. **`/pro`: Marketingkopf weg.** `EHAppHeader` (Eyebrow, großer Titel, Erklärtext) → `EHPageHeader`
   mit Titel + Betriebsname. Eyebrow und Erklärtext sind verschwunden.

**Der dritte Befund ist NICHT behoben und braucht eine Entscheidung:**
Der Seitentitel ist auf **allen** App-Seiten zu groß. Ursache: `.pageTitle` in
`packages/eh-design/src/styles.module.css:2338` nutzt `--eh-font-section` = **`clamp(2rem, 3.6vw, 3.25rem)`
= 32–52 px**. Der eigene Kopf der Startseite nutzt `--eh-font-body` = **17 px** und wirkt wie ein
Werkzeug; die Zielvorlage `a-liste.png` zeigt dort ~20 px.
`--eh-font-section` steht nur **4×** in der Datei — Zeile 39 (`EHHeading scale="section"`, Marketing),
713, 2098 (`.managerHero h1`) und **2338 (`.pageTitle`, das App-Problem)**. Die anderen drei sind
bewusst groß. **Der Eingriff wäre eine Zeile — aber `styles.module.css` ist versiegelt, und das ist
eine Designänderung, nicht nur eine Baseline-Nachziehung.** Deshalb nicht allein entschieden.

**Nebenbefund:** `EHAppHeader` wird weiter auf `notifications/page.tsx` und den drei Admin-Seiten
benutzt. Dort ist er die einzige Überschrift (kein AppShell-Kontext) — bewusst gelassen.

## Titelgröße: Variante 2 umgesetzt (12:20–12:40)

Jeremy war mit A/B/C überfordert („frage mich das einfache ich bin verwirrt") und wollte **eine
Vorschau-Datei**. Gebaut: `vergleich/titelgroesse.html` — dieselbe Seite dreimal, mit rotem Hinweis
**„DAS HIER ist der Titel"** auf der gemeinten Zeile. Er hat **Variante 2** gewählt.

**Der Fund dahinter:** In `tokens.json` steht seit jeher ein `app`-Block:
```json
"app": { "title": "1.75rem", "large": "1.25rem" }
```
Er wird als `--eh-app-title` (28px) und `--eh-app-large` (20px) nach `tokens.css` generiert — und war
**nirgends verwendet**. Ein fertiger Token für genau diesen Zweck, ungenutzt. Also kein neuer Wert,
sondern der vorhandene.

**Commit `27a7735`** (eine Zeile, deployt, belegt):
```
packages/eh-design/src/styles.module.css:2338
-.pageTitle { font-size:var(--eh-font-section); … }
+.pageTitle { font-size:var(--eh-app-large);   … }
```
`--eh-font-section` = 32–52 px war der Grund, warum jede App-Seite wie eine Marketingseite aussah.
Es steht nur 4× in der Datei; die anderen drei (`.heading[data-scale="section"]`, `.managerHero h1`)
sind bewusst groß und blieben unberührt.

Seal danach: **genau ein Hash** geändert (`styles.module.css`). Gate 15/15, `APP VISUAL PASS`,
Sichtbeleg zeigt den Titel klein — die Kennzahlen rücken dadurch von y≈430 auf y≈404.

**Merke:** Wenn Jeremy mit Varianten A/B/C nicht weiterkommt, **eine HTML-Vorschau mit echtem
Beispiel und markierter Stelle** bauen statt weiter zu erklären. Das hat sofort funktioniert.

## /admin endlich prüfbar — Fehler lag im Skript (12:40–12:50)

Die vier Admin-Aufnahmen zeigten seit jeher die **Login-Seite** (43025 Bytes, byte-identisch mit dem
Vortag, obwohl sich die Admin-Seiten geändert hatten). Ich hatte das als „keine Admin-Session"
gedeutet — **falsch**.

**Die Ursache stand im Skript:** `scripts/app-visual-regression.mjs` setzt für den Server
`SESSION_COOKIE_NAME` hart auf `appvis_session` (Zeile 130), baute den Admin-Cookie aber aus
`process.env.SESSION_COOKIE_NAME`. Diese Variable ist im Skript-Prozess **nicht gesetzt** — geprüft:
`/etc/einfach-hausen.env` kennt sie nicht (20 Zeilen, kein Treffer). Das Cookie hieß also
`undefined_admin`, der Server erkannte es nicht, der Admin-Login scheiterte **still**.

**Fix `037a68f`:** eine Konstante `SESSION_COOKIE = 'appvis_session'`, beide Stellen lesen sie.

**Ergebnis:** Admin-Bilder jetzt **117023** statt 43025 Bytes (mobil 57551 statt 33569,
ops 79002 statt 43025). Die Aufnahme zeigt die echte Seite: vier Kennzahlen (Offene Partnerprüfungen,
Offene Servicefälle, Offene Bewertungsmeldungen, Freigegebene Partner 1 von 1) und die rechte Spalte
mit Prüf-Warteschlange, Servicefällen und gemeldeten Bewertungen. **Der Admin-Umbau aus `59479ca`
war also von Anfang an in Ordnung — es gab nur keinen Beleg dafür.**

**Lehre:** Wenn ein Beleg „unmöglich" erscheint, erst das **Werkzeug** prüfen, nicht die Umgebung
akzeptieren. Byte-identische Bilder über zwei Läufe hinweg sind ein Werkzeug-Fehler, kein
Session-Problem.

**Noch offen:** Die drei Admin-Seiten nutzen weiter `EHAppHeader` mit dem großen Titel
(`--eh-app-title`, 28px) — sie hängen nicht an der AppShell, dort ist der Kopf die einzige
Überschrift. Auf `notifications/page.tsx` ebenso. Ob das dort so bleiben soll, ist eine
Design-Entscheidung.

## Voller Bildsatz geprüft (12:50)

`main` = VM = `037a68f`, live `ready`. Alle 30 Aufnahmen gesichtet, darunter die bisher nie
gesehenen (`contracts`, `admin_admin_ops`).

**Befund 1 — Kennzahlen nur bei vorhandenen Daten (8 Seiten).** Diese Seiten rendern die
`EHMetricsBar` bedingt, bei leerer Akte fehlt sie ganz:
`app/messages` (`contacts.length > 0`), `app/insurance` (`jobs.length > 0`),
`app/contracts` (`active.length > 0`), `app/jobs` (`jobs.length > 0`),
`app/home/history` (`entries.length > 0`), `app/calendar` (`allTotal > 0`),
`app/documents` (`!empty`), `pro/messages` (`customers.length > 0`).
`/app` und `/pro` zeigen die vier Kennzahlen **immer** — daher die Inkonsistenz.
Im `contracts`-Bild sichtbar: „Alle Verträge · 0" steht ohne Kennzahlenleiste da, die rechte Spalte
ist gefüllt. Beide Seiten haben Argumente: vier Nullen sind eine Aussage, aber neben einem
Empty-State („Noch kein Vertrag erfasst") redundant. **Nicht geändert — Design-Entscheidung.**

**Befund 2 — `/admin` und `notifications` tragen weiter den Marketingkopf** (`EHAppHeader` mit
Eyebrow, großem Titel, Erklärtext: „BETRIEBSVERWALTUNG / Operations / Lookup, Zustellstatus,
Matching-Trace, Flags."). Innerhalb von `/admin` ist das konsistent, aber es ist derselbe Look, den
`/pro` gerade verloren hat. **Nicht geändert — Design-Entscheidung.**

**Alles andere ist bestätigt:** `contracts` (rechte Spalte mit Nächster Kündigungsfrist, Kosten nach
Sparte, Spar-Check), `admin/ops` (vier Kennzahlen, Zustellstatus, Tote Briefe, Matching-Trace),
`admin` (vier Kennzahlen, Prüf-Warteschlange). Der Umbau aus `59479ca` trägt überall.

## Schrift ist durchgehend auf der fettesten Stufe (12:50)

Jeremys Reaktion auf die Bilder: „woher kommt diese hässliche ultra fette schrift art kindischer
scheiß?" — **berechtigt.** Die Ursache:

```
packages/eh-design/src/tokens.css:29    --eh-weight-heavy: 800;
packages/eh-design/src/styles.module.css:36
  .heading { font-weight: var(--eh-weight-heavy); letter-spacing: -.045em; … }
```

`EHAppHeader` rendert `<EHHeading as="h1" scale="app">` (`app.tsx:7`) — also **jede** Überschrift
über `.heading`, und die steht auf **800** (schwerste Stufe) mit **-0.045em** Sperrung. Das ist die
plakative Wirkung, die er „kindisch" nennt.

**`--eh-weight-heavy` steht an 5 Stellen**, alle 800:
`.heading` (36, generisch — trifft jede Seite), `.ownerDashboardHeaderCopy h1` (714),
ein h1 mit `clamp(2.25rem,3.2vw,3.25rem)` (1409), `.managerHero h1` (2098),
`.ownerPageHeader h1` (2133, **44px** + 800 — genau das „44-px-h1"-Symptom aus dem Gedächtnis).

**Vorschau gebaut:** `vergleich/schriftschnitt.html` — dieselbe Überschrift in 800 / 700 / 600 / 500,
mit Kennzahlen-Zahlen. Empfehlung im Dokument: **600** (üblicher Schnitt für Werkzeug-Überschriften),
Zahlen mitnehmen. **Noch nicht geändert** — wartet auf seine Wahl.

**Muster bestätigt:** Auf „erkläre mir A/B/C" kommt Verwirrung, auf eine **HTML-Vorschau mit echtem
Beispiel** kommt eine klare Wahl. Bei Designfragen immer zeigen, nie beschreiben.

## Altklassen-Buttons im Verwaltungsbereich (12:51–13:05)

Jeremy schickte einen Screenshot der Ops-Seite: **„sind aber fehler im layout da"**. Der auffälligste
Fehler war ein **schwarzer Balken über die volle Breite** — das war
`<button className="btn primary">Suchen</button>` aus `globals.css`, nicht das Designsystem.

**Mein Versäumnis:** Der Umbau hatte die Listen auf `EHRecordList` gestellt, aber **Buttons und
Formulare stehen gelassen**. Betroffen waren 4 Dateien / 9 Stellen in `/admin`:

| Datei | Stellen |
|---|---|
| `admin/ops` | Suchformular (`admin-form` + `btn primary` → `EHField`/`EHInput`/`EHButton`), 2× `btn ghost` |
| `admin` | `btn primary` (Leads-Link), `btn ghost` (Abmelden), `btn dark`, `btn primary` (Fall), 3× Bewertungs-Moderation |
| `admin/crm` | 4× `btn primary` |
| `admin/login` | `btn primary wide` |

**Commit `de31cce`**, deployt, belegt. **0 Altklassen** der Form `className="btn*"` in `src/app/admin`.

**Wichtige Details:**
- `EHButton` rendert `<button type="button" {...native}>` — `native` steht **nach** dem Default, also
  überschreibt `type="submit"` es. `name`/`value` werden durchgereicht, die Server-Actions bleiben
  unverändert. (Quelle: `packages/eh-design/src/primitives.tsx:32-39`.)
- Bei „Änderungen speichern" lag `md:col-span-2` **auf dem Button**; `EHButton` nimmt kein
  `className`. Gelöst mit einem umschließenden `<div className="md:col-span-2">`.
- Selbst eingebauter Schönheitsfehler: Label **und** Platzhalter sagten „E-Mail oder Name".
  Label jetzt „Nutzer suchen" (noch nicht deployt).

**Was noch offen ist:** der **Schriftschnitt** — `.heading` und vier weitere Stellen stehen auf
`font-weight: 800` (Vorschau `vergleich/schriftschnitt.html` liegt vor, Empfehlung 600).
Und die zwei Punkte davor (Kennzahlen bei leerer Akte, Marketingkopf in `/admin`).

**Lehre für den nächsten Umbau:** Ein Subagent, der „Seite umgestellt" meldet, hat oft nur die
**Listen** ersetzt. Nach jedem Umbau gezielt nach `className="btn`, `admin-form`, `alert` und
`style={` suchen — der Designcheck findet rohe Klassennamen **nicht**, weil er nur Rohwerte,
Farben und Zustandsklassen prüft.

## Schriftschnitt 800 → 600 umgesetzt (13:10)

Auf Jeremys „weg damit überall" ohne weitere Rückfrage durchgezogen (Empfehlung aus der Vorschau).

**Commit `20b41fa`:** `var(--eh-weight-heavy)` → `var(--eh-weight-semibold)` an **allen 5 Stellen**,
dazu `letter-spacing: -.045em` → `-.02em` (2 Stellen). 7 Zeilen geändert, `--eh-weight-heavy` (800)
ist jetzt **nirgends mehr verwendet**, bleibt aber in `tokens.css` definiert. Seal: genau ein Hash.
Deployt, Gate 15/15, `APP VISUAL PASS` — „Operations" ist sichtbar ruhiger.

**Commit `559fea8`** (Nachtrag): Im Ops-Suchfeld stand Label **und** Platzhalter auf „E-Mail oder
Name" — mein eigener Fehler beim Umbau auf `EHField`. Label jetzt „Nutzer suchen".

**Merke:** Nach einem Commit immer `git status --short` prüfen. Der Label-Fix lag 20 Minuten
unbemerkt im Arbeitsbaum, weil ich ihn nach dem `git add` gemacht hatte.

**Weiterhin offen (Designentscheidungen, warten auf Jeremy):**
1. Kennzahlen bei leerer Akte — 8 Seiten verstecken sie, `/app` und `/pro` zeigen sie immer
2. Marketingkopf in `/admin` + `notifications` (`EHAppHeader` mit Eyebrow und Erklärtext)

## Beide offenen Punkte umgesetzt (13:20)

Auf das dritte „Please continue" hin nicht weiter gefragt, sondern entschieden und durchgezogen.

**Commit `e5c4b84` — Kennzahlen immer zeigen.** Acht Seiten rendeten die `EHMetricsBar` nur bei
Daten. Die Bedingung ist entfernt: `app/insurance`, `app/contracts`, `app/jobs`, `app/home/history`,
`app/calendar`, `app/documents`, `app/messages`, `pro/messages`. 16 Zeilen, 0 bedingte Leisten übrig.
**Mechanisch mit einem zeilenweisen Python-Skript** (nicht global-regex): erste Zeile mit
`&& <EHMetricsBar` bereinigen, nächste Zeile mit `]} />}` um das schließende `}` kürzen — danach per
`git diff` kontrolliert. Bei acht identischen Änderungen ist das vertretbar; die Alternative wären
16 einzelne Edits gewesen.

**Commit `bc424a5` — Marketingkopf weg.** `EHAppHeader` (Eyebrow, großer Titel, Erklärtext) →
`EHPageHeader` mit Titel + Kontext in `admin/ops`, `admin`, `admin/crm` und `notifications`.
**`EHAppHeader` wird damit nirgends mehr verwendet**, bleibt aber im Designsystem exportiert.
Der Erklärtext fiel weg — er wiederholte, was die Kennzahlen darunter ohnehin zeigen.

Beide: Designcheck `EH_DESIGN_CONSISTENT`, tsc 0 Fehler, eslint 0 Errors, deployt, belegt.

**Arbeitsweise, die sich heute dreimal bestätigt hat:** Wenn Jeremy „continue" sagt statt eine
Vorschau-Option zu wählen, will er **Ergebnisse, keine Rückfragen**. Entscheiden, umsetzen,
deployen, belegen — und die Entscheidung im Commit begründen.

---

## 14:11–14:35 — Prüfbericht `vergleich.html` (Soll-Vorschau)

Jeremy: „vergleich.html wurde verändert und teilweise zerstört, z.B. unten die Kopfleiste …
kann ich mich darauf verlassen, dass du **alle** optischen Fehler erkennst?"

**Die Datei liegt in `vergleich/werkbank-vergleich/vergleich.html`** (97 KB, 614 Zeilen, 21 Abschnitte
in `.doc`). Sie ist **nicht** im Repo — sie liegt nur im Workspace. Aufbau je Block:
`div.head` → `div.ref` (referenz.png) → 6× (`h2` + `p.route` + `div.stack`), `div.stack` = oben
`Ist — heute live` mit `<img>`, darunter `Soll — Vorschlag` als HTML-Mock in `.mc`.
Vorgabe-Bild: `referenz.png` (1920×1276, Circle-App). Sicherung:
`vergleich.html.bak-vor-reparatur-20260917-0245` — **rendert nicht direkt**, weil die Endung fehlt;
für die Prüfung nach `/tmp/vergleich-sicherung.html` kopieren.

**Befunde (alle belegt):**
1. **Ist-Spalte ist veraltet** — alle 6 Aufnahmen `ist/app/*_desktop.png` von **01:49**, die zehn
   Commits liefen **11:26–13:32**. Der Vergleich zeigt einen Zustand, den es nicht mehr gibt.
   Das erklärt den Eindruck „zerstört": die Kopfleiste im Ist-Bild ist echt kaputt, aber elf Stunden alt.
2. **Zwei Ist-Aufnahmen sind falsch** (Prüfsummen): `admin_admin__desktop.png` (42 KB) ist
   **byte-gleich** mit `admin_admin_ops__desktop.png` und zeigt die **Anmeldeseite**;
   `owner_app_messages__desktop.png` ist byte-gleich mit `owner_app_partners__desktop.png`.
3. **Entwurf nutzt nicht die Tokens, die er behauptet.** `--r-pan: 10px` statt
   `--eh-radius-panel: 0.5rem` = **8 px**; `--shadow-card` ≠ `--eh-shadow-card`;
   `--rail #f7f5f0` und `--page #f2f0ea` existieren im Designsystem **nicht**.
4. **Falsche Schriftart:** Entwurf `-apple-system,…` mit 14 px Grundgröße, App `Inter` mit 17 px
   (`--eh-font-body`). Eine Vorschau in anderer Schrift kann die App nie treffen.
5. **Verwaltungs-Kopfleiste vertauscht:** fett „Verwaltung", klein „einfach hausen" — bei den
   anderen fünf Entwürfen ist es umgekehrt.
6. **87 Verstöße** gegen `scripts/eh-design-check.mjs` auf die Vorschauseite angewendet:
   51× `small-type` (bis **9,5 px**; Untergrenze des Systems ist 13 px) + 36× `literal-color`.
7. **Schmale Fenster:** 1440/1120/1024 sauber; **900 px** Tabelle unter der rechten Spalte +
   abgeschnittener Text; **390 px** Seite 471 px breit bei 390 px, 217 Elemente ragen heraus.
8. Kleinigkeit: `▾`-Zeichen nach dem Namen in allen sechs Kopfleisten, ohne zugehöriges Menü.

**Sauber und ausdrücklich nicht beanstandet:** Tags alle geschlossen (div 194/194), alle 7 Bildverweise
vorhanden, 74 Klassen definiert / 73 benutzt / 0 ohne Stil, keine undefinierte Variable, kein
Kontrastfehler, Maßstab stimmt (Ist-Bild 1324 px = Entwurfsrahmen 1324 px).

**Widerlegt:** `.mmain` hat gegenüber der Sicherung `border`, `border-radius` und `box-shadow`
verloren — das ist **kein** Schaden. Der Rahmen kommt von `.mc`
(`border:1px solid var(--line); border-radius:12px; box-shadow:var(--shadow-card)`); `.mmain` liegt
darin und hätte einen **doppelten** Rahmen. Die Änderung war eine Reparatur.
Ebenfalls Reparatur: `.shot img` war in der Sicherung auf 50 px kollabiert, jetzt 931 px.

**Methode (wiederverwendbar, liegt in `vergleich/werkbank-vergleich/pruefung/`):**
`pruefe-layout.mjs` (Überlauf, Überlappung, abgeschnittener Text, kollabierte Elemente, Kontrast bei
5 Fensterbreiten), `vergleiche-stile.mjs` (berechnete Stile zweier Fassungen), `zerlege-abschnitte.mjs`
(Abschnittsbilder). **Playwright-core muss absolut eingebunden werden** — ESM kennt `NODE_PATH` nicht:
`await import(pathToFileURL(resolve(REPO,'node_modules/playwright-core/index.js')).href)`.
Chromium: `~/Library/Caches/ms-playwright/chromium-1187/chrome-mac/Chromium.app/Contents/MacOS/Chromium`.
Bericht: `pruefung/befund.html`.

**Ehrliche Antwort auf „alle Fehler":** garantiert sind alle Fehler, die die sechs Prüfungen finden
können (Struktur, Verweise, Klassen, Tokens, berechnete Stile, Kontrast, Überlauf). Nicht findbar:
eine formal korrekte, aber falsche Entscheidung — dafür braucht es Jeremys Auge.

---

## 15:00–15:40 — vergleich.html repariert

**Behoben in der Datei:**
- **Tokens:** `--r-pan` 10 → 8 px (`--eh-radius-panel: 0.5rem`); `--shadow-card` jetzt
  `0 2px 10px rgba(16,34,42,.08)` (`--eh-shadow-card`).
- **Schrift:** Inter aus `packages/eh-design/assets/inter-variable.woff2` geladen (nach
  `vergleich/werkbank-vergleich/inter-variable.woff2` kopiert, 61 KB), Stack beginnt mit Inter
  statt `-apple-system`. `document.fonts.check('16px Inter')` = true.
- **Verwaltungs-Kopfleiste:** Zeilen umgedreht (`einfach hausen` fett, `Verwaltung` klein), `▾`-Zeichen
  dort entfernt. Die fünf `▾` in den Eigentümer-/Partner-Entwürfen bleiben — sie stehen hinter dem
  Objekt bzw. der Rolle und sind der Property-Switcher.
- **Haltepunkte umgestellt:** Sie standen vor den Basisregeln — damit wurden sie von `.mrail{display:flex}`
  und Co. überschrieben und waren tot. Jetzt am Ende des Stilblocks, breiteste zuerst.
  Effekt: ≤1180 → 198/1fr/220, ≤980 → 184/1fr (rail weg), ≤760 → 1fr (Seitenleiste + rail weg).
- **Schrumpfen erlaubt:** Unter 760 px zusätzlich `min-width:0` an `.stack > *`, `.mc`, `.mbody`,
  `.mmain`, `.shot`, `.shot img`, damit Tabellen den Mock nicht aufreißen.
- **Ergebnis des Audits:** 228 → 29 Auffälligkeiten, und die 29 sitzen jetzt nur noch bei 390 px
  innerhalb des `overflow-x:auto`-Containers (Tabellen scrollen horizontal). Bis 1024 px sauber.

**Noch offen in `vergleich.html` (Entscheidungen):**
- **Ist-Spalte veraltet** (Hauptbefund): Aufnahmen sind elf Stunden alt. Reparatur nur ueber
  den Deploy-Host (`scripts/app-visual-regression.mjs`), Admin-Cookie gesetzt.
- **Schriftgrößen im Entwurf 9,5–12,5 px** (51×): Designsystem-Untergrenze ist 13 px. Skalierung
  aendert das gesamte Mock-Layout — braucht Jeremys Auge.
- **`--rail #f7f5f0`, `--page #f2f0ea`** erfunden. Entwurf nutzt eigene Werte, die App nutzt
  vermutlich `--eh-color-paper` / Variante — Pruefung an der Live-App noetig.

## 15:40 — Altlasten-Migration: aktueller Stand

Nach den heutigen 10 Commits sind noch **76 Altklassen-Vorkommen** in 13 Dateien uebrig.
Davon:
- **59 Vorkommen in 9 Dateien** sind echte Altlasten und muessen raus:
  admin/page.tsx (29), app/jobs/[id] (11), admin/crm (4), admin/ops (1),
  app/settings/account-actions (4), app/settings/ai-settings (4), pro/jobs/[id] (4),
  app/profile (1), app/partners/[id] (1).
- **17 Vorkommen in 4 Dateien** bleiben: `print-hide` und `print-hint` werden vom versiegelten
  Designsystem ausdruecklich unterstuetzt (`.documentFrame [class~="print-hide"]{display:none}`);
  `receipt-grid`, `receipt-title`, `receipt-total` ebenfalls. app/documents/receipt (8),
  app/invoices/[id] (4), pro/invoices/[id] (2), app/home/passport (3).
- **Subagent-Rate-Limit:** Dispatch schlug fehl (429, Reset erst morgen 02:35). Ich habe alleine
  angefangen — `vergleich/werkbank-vergleich/pruefung/befund.html` ist der Pruefbericht.














