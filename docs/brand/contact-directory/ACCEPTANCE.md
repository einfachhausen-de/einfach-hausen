# Gina Ansprechpartner — binding acceptance and scope decisions
Authority: operator's latest message relaying Gina Schulze, founder and owner. Overrides prior contacts-first design for /app/messages. This request belongs to existing Einfach Hausen product goal.

1. Root route shows exactly the 17 specified main categories in supplied order. No contact names, subtrades, contact tiles, mixed category/contact search results, or automatic expanded sections on this level. Existing app shell/navigation remains accessible.
2. Main-category route shows ONLY that main category's complete subcategory selection. Back/breadcrumb preserve parent. Opening a subcategory reveals its contacts and add-contact entry point. A deliberate all-contacts-in-main view may aggregate by unique ID, never default to violating the level rule.
3. Opening the add flow inside a subcategory carries that exact subcategory preselected. Other subcategories in same main are optional unchecked choices; suggestions are never asserted as verified services or silently selected. Original subcategory-only save works. No pressure to select multiple.
4. One contact identity, separate many-to-many association links. Repeated saves/additions and concurrent duplicate requests must be idempotent. Unique constraints enforce no duplicate association. Contact name alone is NOT a safe unique identifier; match existing IDs first, show possible duplicates based verified contact fields without merging different people automatically.
5. A saved contact selected in three subcategories has ONE contact record and THREE association rows. It appears once in each selected leaf and once with three readable service labels in an aggregate view. Editing the same contact changes shared details for all appearances. Existing messages/history remain linked to original identity.
6. Editor shows all options of the selected main category, current choices checked. Add/remove associations atomically, validate all IDs server-side, enforce ownership/tenant scope and same-main selection within submitted group. Editing one group must not erase other groups' assignments. Empty-set handling must be explicit (remove category membership vs delete contact) and never silently delete contact/history.
7. Existing contacts/custom categories preserved during migration. Only exact unambiguous mapping is automatic; retain legacy category data for uncertain mappings and expose explicit assignment repair on contact access. Never fabricate provider logins or verified service claims for external/manual contacts.
8. Tests: exact17 first-level nodes; no contacts/subcategories first-level (negative test with seeded contacts); each root opens only own subcategories; leaf routing and back paths; create 1+3; single-subcategory save; repeated/concurrent saves; editing remove1+add1; foreign user/category rejection with DB unchanged; message/history continuity; unknown legacy mapping preserved; typed/pasted inputs rendered as text; same-name distinct real contacts not merged.
9. Visual reference is newly root-authored, not an endorsement of current system. Keep original logo, no illustration/emoji sticker wall, no repeated icon plaques; exact category labels preserved, compact coherent category navigation and task-focused add/edit UI. Explicitly compare desktop/tablet/mobile; real filled forms, multiselect, validation/error/empty/loading states, keyboard focus and long category names. Root supplies full UI source, workers no visual improvisation.
10. Delivery: one implementation writer with complete root packet, parallel independent review; one batched real-browser verification + one bounded correction pass. Native OCI environment, preserve data, migration rollback/backups, no red-CI override, release via GitHub and normal deployment, actual live owner+provider checks. No false done from build/unit tests or intent. New app-wide style not silently propagated before the reference is reviewed.

Open facts, not questions to human: authoritative complete 17-subcategory taxonomy; actual manual-contact identity schema; canonical current task/worktree availability. Free scouts resolve from repository, proposing missing taxonomy explicitly as proposal.
